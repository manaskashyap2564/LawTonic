import 'package:flutter/material.dart';

class CyberLawPage extends StatelessWidget {
  const CyberLawPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cyber Law'),
      ),
      body: const Center(
        child: Text('Cyber Law Content'),
      ),
    );
  }
}
